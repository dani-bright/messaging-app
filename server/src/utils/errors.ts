export enum AuthError {
  STRATEGY_INVALID = 'auth.login.strategy.invalid',
  STATUS_INVALID = 'auth.login.status.invalid',
  EMAIL_NOT_FOUND = 'auth.login.email.notFound',
  PASSWORD_INVALID = 'auth.login.password.invalid',
}
